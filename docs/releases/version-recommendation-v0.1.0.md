# Version Recommendation

## Recommended Stable MVP Version
- `v0.1.0`

## Rationale
The repository manifests and application packages are already aligned on version `0.1.0`, and the current implementation scope matches the definition of a first stable MVP release.

## Suggested Git Tag Commands
```bash
git tag -a v0.1.0 -m "Castaminofen MVP Release"
git push origin v0.1.0
```
